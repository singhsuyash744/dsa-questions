/* ques link
https://www.geeksforgeeks.org/problems/queue-reversal/1
/*

SOLN
class Solution {
    public void reverseQueue(Queue<Integer> q) {

        if (q.isEmpty()) {
            return;
        }

        int front = q.remove();

        reverseQueue(q);

        q.add(front);
    }
}
